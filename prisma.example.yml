endpoint: http://localhost:4466
datamodel:
  - prisma/users.prisma
  - prisma/content.prisma

secret: please-change-me

generate:
  - generator: javascript-client
    output: ./prisma/client

hooks:
  post-deploy:
    - prisma generate
