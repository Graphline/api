# import Crud from '../interface/crud.graphql'

type Permission implements Crud {
  id:        ID!
  createdAt: DateTime!
  updatedAt: DateTime!
  createdBy: User!
  updatedBy: User!
  display:   String!

  value: String!
}
